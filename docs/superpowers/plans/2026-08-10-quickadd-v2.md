# 빠른 추가 v2(한눈에 보기) + 프로젝트 검색 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 빠른 추가에 담당자·상태·우선순위·시작일·마감일을 접지 않고 한 화면에 펼치는 두 번째 레이아웃을 더하고(설정에서 선택), 프로젝트 선택을 검색되는 드롭다운으로 바꾼다.

**Architecture:** `src/quickadd/main.ts`(723줄, 상태·DOM·창 크기·키보드가 뒤섞임)를 *상태*(`state.ts`, 순수)와 *렌더러*(`layoutCompact.ts` / `layoutExpanded.ts`)로 가른다. 두 렌더러는 같은 `LayoutHandle` 인터페이스로 `main.ts`에 붙고, 제출·창 크기·전역 키보드 계약은 `main.ts`에 남는다. 프로젝트 선택은 두 레이아웃이 공유하는 `projectPicker.ts`다.

**Tech Stack:** TypeScript + Vite (프론트), Rust + Tauri 2 (백엔드), vitest (테스트), pnpm.

**설계 스펙:** `docs/superpowers/specs/2026-08-10-quickadd-v2-design.md`
**목업(확정, 커밋됨):** `docs/mockups/quickadd-v2-mockup.html` — v2 마크업·CSS·상호작용의 **기준**이다. CSS를 새로 지어내지 말고 이 파일에서 옮겨라.

## Global Constraints

- **테스트 환경에 DOM이 없다.** vitest가 node 환경으로 돌고 jsdom/happy-dom이 설치돼 있지 않다(`vite.config.ts`에 test 설정 없음, `package.json`에 jsdom 없음). 이 저장소의 모든 테스트는 순수 함수 테스트다. **DOM을 만지는 테스트를 쓰지 마라** — 순수 로직을 파일로 빼서 테스트하고, 화면은 앱을 띄워 눈으로 확인한다.
- **테스트 명령:** 전체 `pnpm test`, 파일 하나 `pnpm vitest run <경로>`.
- **`pnpm build`는 타입 검사를 하지 않는다** — `vite build`뿐이다. 타입은 따로 봐야 한다: `pnpm exec tsc --noEmit`.
- **시작 시점에 기존 타입 에러가 하나 있다** (내가 만든 게 아니다): `src/quickadd/main.ts(616,19) TS2345 — 'string | boolean' is not assignable to 'boolean'`. 새 DOM 타입에서 `HTMLElement.hidden`이 `boolean | "until-found"`가 되면서 `setDropdownOpen(dropdown.hidden)`이 걸린 것이다. **Task 2가 그 줄을 지우면서 사라진다.** Task 2 Step 5부터 `tsc --noEmit`은 깨끗해야 한다 — 그 뒤로도 이 에러가 남아 있으면 삭제를 빠뜨린 것이다.
- **앱 실행:** `pnpm tauri dev`. 빠른 추가는 기본 단축키 **F1**으로 연다.
- **주석과 UI 문구는 한국어.** 기존 코드의 주석 밀도·어투를 따른다(왜 그렇게 했는지를 적는다).
- **CHANGELOG 규칙**(`CLAUDE.md`): 사용자에게 보이는 변경은 **같은 커밋에** `CHANGELOG.md`의 `## [Unreleased]`에 한국어 한 줄. 카테고리는 `### 추가` / `### 변경` / `### 수정`만. 내부 리팩터는 기록하지 않는다. 이 계획에서 CHANGELOG 줄이 붙는 커밋은 **Task 2와 Task 7 딱 둘**이다.
- **창 폭:** 컴팩트 레이아웃 540px, 한눈에 보기 레이아웃 660px.
- **담당자 인라인 칸 수:** 4칸("나" 포함).
- **프로젝트 드롭다운:** 폭 272px, 목록 `max-height: 208px`.
- **설정 기본값:** `quickadd_layout = "compact"` (자동 업데이트로 창 모양이 말없이 바뀌면 안 된다).
- **커밋은 각 Task 마지막 단계에서 한 번.** Task를 걸쳐 커밋을 미루지 않는다.

## 파일 구조

| 파일 | 책임 | Task |
|---|---|---|
| `src/shared/projectSearch.ts` (신규) | 프로젝트 이름 매칭(부분 일치 + 초성). 순수. | 1 |
| `src/shared/projectSearch.test.ts` (신규) | 위 테스트 | 1 |
| `src/quickadd/projectPicker.ts` (신규) | 검색되는 프로젝트 드롭다운. 두 레이아웃 공용. | 2 |
| `src/shared/app.css` (수정) | 드롭다운 검색 UI, v2 필드 격자 스타일 | 2, 7 |
| `src/quickadd/state.ts` (신규) | 폼 상태와 변경 함수. DOM을 모른다. | 3 |
| `src/quickadd/state.test.ts` (신규) | 위 테스트 | 3 |
| `src/quickadd/layout.ts` (신규) | `LayoutHandle` / `LayoutContext` / `LayoutHosts` 인터페이스만 | 4 |
| `src/quickadd/layoutCompact.ts` (신규) | v1 칩 행 렌더러(기존 코드 이동) | 4 |
| `src/quickadd/assigneeSlots.ts` (신규) | 담당자 인라인/접힘 배치. 순수. | 5 |
| `src/quickadd/assigneeSlots.test.ts` (신규) | 위 테스트 | 5 |
| `src/quickadd/layoutExpanded.ts` (신규) | v2 필드 격자 렌더러 | 7, 8 |
| `src/quickadd/index.html` (수정) | 레이아웃이 채울 빈 host 두 개로 정리 | 4 |
| `src/quickadd/main.ts` (수정) | 부팅·레이아웃 선택·창 크기·제출·전역 키보드 | 2, 3, 4, 7, 8 |
| `src-tauri/src/config.rs` (수정) | `quickadd_layout` 설정 필드 | 6 |
| `src-tauri/src/commands.rs` (수정) | DTO·저장·`settings-changed` 이벤트 | 6 |
| `src/shared/types.ts` (수정) | `SettingsDto`에 필드 추가 | 6 |
| `src/shared/ipc.ts` (수정) | `saveSettings` 인자 추가 | 6 |
| `src/settings/index.html`, `main.ts` (수정) | 레이아웃 선택 select | 6 |

---

### Task 1: 프로젝트 이름 매칭 로직

**Files:**
- Create: `src/shared/projectSearch.ts`
- Test: `src/shared/projectSearch.test.ts`

**Interfaces:**
- Consumes: `Project` from `src/shared/types.ts` (`{ id, name, identifier }`)
- Produces:
  - `interface ProjectMatch { project: Project; range: [number, number] | null }`
  - `function filterProjects(projects: Project[], query: string): ProjectMatch[]`

`range`는 **`project.name` 안의** 맞은 구간 `[시작, 끝)`이다. 검색어가 비면 `null`. Task 2가 이 구간을 `<mark>`로 감싼다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

Create `src/shared/projectSearch.test.ts`:

```ts
import { describe, it, expect } from "vitest";
import { filterProjects } from "./projectSearch";
import type { Project } from "./types";

const P = (name: string): Project => ({ id: name, name, identifier: name.slice(0, 2) });
const PROJECTS = [
  P("Plane Quick Dock"),
  P("디자인 시스템"),
  P("QA 자동화"),
  P("데이터 파이프라인"),
];

describe("filterProjects — 빈 검색어", () => {
  it("전체를 순서 그대로 돌려주고 강조 구간은 없다", () => {
    const out = filterProjects(PROJECTS, "");
    expect(out.map((m) => m.project.name)).toEqual([
      "Plane Quick Dock", "디자인 시스템", "QA 자동화", "데이터 파이프라인",
    ]);
    expect(out.every((m) => m.range === null)).toBe(true);
  });
  it("공백만 있어도 빈 검색어로 본다", () => {
    expect(filterProjects(PROJECTS, "   ")).toHaveLength(4);
  });
});

describe("filterProjects — 부분 일치", () => {
  it("이름 중간에 걸려도 남는다", () => {
    const out = filterProjects(PROJECTS, "자");
    expect(out.map((m) => m.project.name)).toEqual(["디자인 시스템", "QA 자동화"]);
  });
  it("맞은 구간을 이름 기준 인덱스로 돌려준다", () => {
    const out = filterProjects(PROJECTS, "자");
    expect(out[0].range).toEqual([1, 2]); // 디[자]인 시스템
    expect(out[1].range).toEqual([3, 4]); // QA [자]동화
  });
  it("영문 대소문자를 가리지 않는다", () => {
    const out = filterProjects(PROJECTS, "pl");
    expect(out.map((m) => m.project.name)).toEqual(["Plane Quick Dock"]);
    expect(out[0].range).toEqual([0, 2]);
  });
  it("맞는 게 없으면 빈 배열", () => {
    expect(filterProjects(PROJECTS, "존재하지않음")).toEqual([]);
  });
});

describe("filterProjects — 초성 일치", () => {
  it("초성만으로 찾는다", () => {
    const out = filterProjects(PROJECTS, "ㄷㅈ");
    expect(out.map((m) => m.project.name)).toEqual(["디자인 시스템"]);
    expect(out[0].range).toEqual([0, 2]);
  });
  it("초성 한 글자는 여러 개를 문다 — 첫 음절만이 아니라 어느 음절이든", () => {
    // "QA 자동화"가 걸리는 건 자'동'화의 ㄷ 때문이다. 부분 일치가 이름 중간에
    // 걸리는 것과 같은 규칙이라 일부러 이렇게 둔다.
    const out = filterProjects(PROJECTS, "ㄷ");
    expect(out.map((m) => m.project.name)).toEqual([
      "디자인 시스템", "QA 자동화", "데이터 파이프라인",
    ]);
  });
  it("이름 중간의 초성도 찾는다", () => {
    const out = filterProjects(PROJECTS, "ㅈㄷㅎ");
    expect(out.map((m) => m.project.name)).toEqual(["QA 자동화"]);
    expect(out[0].range).toEqual([3, 6]); // QA [자동화]
  });
  it("완성된 음절이 섞이면 초성 모드로 빠지지 않는다", () => {
    // "ㄷ자"는 초성 검색어가 아니므로 일반 부분 일치로 본다 — 맞는 이름이 없다.
    expect(filterProjects(PROJECTS, "ㄷ자")).toEqual([]);
  });
});
```

- [ ] **Step 2: 테스트를 돌려 실패를 확인한다**

Run: `pnpm vitest run src/shared/projectSearch.test.ts`
Expected: FAIL — `Failed to resolve import "./projectSearch"`

- [ ] **Step 3: 구현한다**

Create `src/shared/projectSearch.ts`:

```ts
import type { Project } from "./types";

export interface ProjectMatch {
  project: Project;
  /** `project.name` 안에서 맞은 구간 [시작, 끝). 검색어가 비면 null. */
  range: [number, number] | null;
}

/** 한글 초성 19자. 인덱스가 유니코드 음절의 초성 번호와 같은 순서다. */
const CHOSEONG = "ㄱㄲㄴㄷㄸㄹㅁㅂㅃㅅㅆㅇㅈㅉㅊㅋㅌㅍㅎ";
const HANGUL_START = 0xac00;
const HANGUL_END = 0xd7a3;
/** 초성 하나가 담당하는 음절 수 = 중성 21 × 종성 28. */
const SYLLABLES_PER_CHOSEONG = 588;

/** 이름을 초성 문자열로 바꾼다. 한글 음절은 초성 한 글자로, 나머지 문자는 그대로 —
 *  **길이가 원본과 같으므로** 여기서 찾은 인덱스를 그대로 이름의 인덱스로 쓸 수 있다. */
function choseongOf(name: string): string {
  let out = "";
  for (const ch of name) {
    const code = ch.codePointAt(0)!;
    out += code >= HANGUL_START && code <= HANGUL_END
      ? CHOSEONG[Math.floor((code - HANGUL_START) / SYLLABLES_PER_CHOSEONG)]
      : ch;
  }
  return out;
}

/** 검색어가 전부 초성 자모일 때만 초성 모드로 본다. 이 조건이 없으면 "자" 같은
 *  완성 음절 하나가 초성 검색과 섞여 엉뚱한 항목까지 물어온다. */
function isChoseongQuery(query: string): boolean {
  return [...query].every((ch) => CHOSEONG.includes(ch));
}

/** 이름 부분 일치(대소문자 무시) 또는 초성 일치. 입력 순서를 유지한다. */
export function filterProjects(projects: Project[], query: string): ProjectMatch[] {
  const q = query.trim();
  if (!q) return projects.map((project) => ({ project, range: null }));

  const choseong = isChoseongQuery(q);
  const needle = choseong ? q : q.toLowerCase();
  const out: ProjectMatch[] = [];
  for (const project of projects) {
    const hay = choseong ? choseongOf(project.name) : project.name.toLowerCase();
    const i = hay.indexOf(needle);
    if (i !== -1) out.push({ project, range: [i, i + q.length] });
  }
  return out;
}
```

- [ ] **Step 4: 테스트를 돌려 통과를 확인한다**

Run: `pnpm vitest run src/shared/projectSearch.test.ts`
Expected: PASS — 10 tests

- [ ] **Step 5: 커밋한다**

내부 로직이라 CHANGELOG 줄은 붙이지 않는다(Task 2에서 화면에 드러날 때 붙인다).

```bash
git add src/shared/projectSearch.ts src/shared/projectSearch.test.ts
git commit -m "feat: 프로젝트 이름 매칭 로직 추가 (부분 일치·초성)"
```

---

### Task 2: 검색되는 프로젝트 드롭다운

**Files:**
- Create: `src/quickadd/projectPicker.ts`
- Modify: `src/shared/app.css` (`.dropdown` 블록, 현재 220-233행 부근)
- Modify: `src/quickadd/main.ts` (`renderSelected`/`renderDropdown`/`setDropdownOpen`/`projBtn` 배선 제거, 78-84행·185-212행·615-624행)
- Modify: `CHANGELOG.md`

**Interfaces:**
- Consumes: `filterProjects`, `ProjectMatch` (Task 1)
- Produces:
  ```ts
  interface ProjectPickerHandle {
    render(): void;        // 버튼 라벨을 현재 선택으로 다시 그린다
    close(): void;         // 열려 있으면 닫는다
    isOpen(): boolean;
    bottom(): number;      // 드롭다운 아래 끝(px). 닫혀 있으면 0.
  }
  function createProjectPicker(opts: {
    button: HTMLElement;
    host: HTMLElement;
    getProjects: () => Project[];
    getSelectedId: () => string | null;
    onPick: (project: Project) => void;
    onResize: () => void;
  }): ProjectPickerHandle
  ```

**설계 근거(스펙에서):** 열리는 방향은 지금처럼 **아래쪽**이다. 위로 열면 프로젝트가 많을 때 창 위 모서리를 넘어가 클릭할 수 없는 부분이 생긴다(`app.css`에 이미 적힌 이유).

- [ ] **Step 1: CSS를 더한다**

`src/shared/app.css`의 `.dropdown` 규칙을 두 군데 고친다:

1. `width: 230px` → `width: 272px`
2. **`max-height: 240px; overflow-y: auto;` 줄을 지운다.** 예전엔 드롭다운이 목록 하나뿐이라 통째로 스크롤해도 됐지만, 이제 위에 검색창과 아래에 키보드 힌트가 고정으로 붙는다. 바깥에 높이 제한을 남겨두면 안쪽 `.dd-list`(208px)와 **스크롤바가 두 겹으로 생기고** 힌트 줄이 잘린다. 스크롤은 `.dd-list`가 혼자 맡는다.

그리고 `.dd-item.kbd-focus` 규칙 **바로 아래**에 다음을 넣는다. (목업 `docs/mockups/quickadd-v2-mockup.html`의 `/* ===== 검색되는 프로젝트 드롭다운 ===== */` 블록과 같다. 변수 이름이 이미 같으므로 그대로 옮기면 된다.)

```css
.dd-item .nm { min-width: 0; overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }

/* 검색창은 목록 위에 붙어 있고 드롭다운을 열면 커서가 여기 놓인다. 그냥 눌러
   고르던 흐름은 그대로고, 검색은 원할 때만 쓰는 추가 경로다. */
.dd-search {
  display: flex; align-items: center; gap: 7px; padding: 6px 9px; margin-bottom: 5px;
  background: var(--bg); border: 1px solid var(--border); border-radius: 7px;
}
.dd-search:focus-within { border-color: var(--accent); }
.dd-search .sicon { flex: none; color: var(--muted); display: flex; }
.dd-search input {
  flex: 1; min-width: 0; background: transparent; border: none; outline: none;
  color: var(--text); font-size: 12.5px; font-family: inherit; padding: 0;
}
.dd-search input::placeholder { color: var(--muted-2); }
.dd-search .cnt {
  flex: none; font-size: 10.5px; color: var(--muted-2);
  font-variant-numeric: tabular-nums; white-space: nowrap;
}
/* 7줄까지 보이고 그 아래는 스크롤 — 프로젝트가 늘어도 창이 계속 자라지 않는다. */
.dd-list { max-height: 208px; overflow-y: auto; }
.dd-list::-webkit-scrollbar { width: 8px; }
.dd-list::-webkit-scrollbar-thumb { background: var(--border); border-radius: 8px; }
.dd-empty { padding: 16px 9px; font-size: 12px; color: var(--muted-2); text-align: center; }
.dd-foot {
  display: flex; gap: 12px; margin-top: 5px; padding: 6px 9px 2px;
  border-top: 1px solid var(--border); font-size: 10.5px; color: var(--muted-2);
}
.dd-foot kbd { font-size: 9.5px; padding: 0 4px; border-bottom-width: 1px; border-radius: 4px; }
```

`mark`와 `.dd-item.kbd-focus`는 `app.css`에 이미 있다(각각 515행, 233행 부근) — 다시 만들지 마라.

- [ ] **Step 2: `projectPicker.ts`를 쓴다**

Create `src/quickadd/projectPicker.ts`:

```ts
import { filterProjects } from "../shared/projectSearch";
import { colorForId } from "../shared/color";
import type { Project } from "../shared/types";

const SEARCH_ICON =
  `<svg width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round"><circle cx="11" cy="11" r="7"/><path d="m20 20-3.5-3.5"/></svg>`;

export interface ProjectPickerHandle {
  /** 버튼 라벨을 현재 선택으로 다시 그린다. */
  render(): void;
  /** 열려 있으면 닫는다. */
  close(): void;
  isOpen(): boolean;
  /** 드롭다운의 아래 끝(px). 닫혀 있으면 0 — 창 높이 계산에 쓴다. */
  bottom(): number;
}

const escapeHtml = (s: string) =>
  s.replace(/[&<>"]/g, (c) => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;" }[c]!));

/** 맞은 구간만 <mark>로 감싼다 — 앞글자가 아니라 이름 어디에 걸렸는지 눈에 보이게. */
function markName(name: string, range: [number, number] | null): string {
  if (!range) return escapeHtml(name);
  const [a, b] = range;
  return escapeHtml(name.slice(0, a)) + "<mark>" + escapeHtml(name.slice(a, b)) + "</mark>" + escapeHtml(name.slice(b));
}

export function createProjectPicker(opts: {
  button: HTMLElement;
  /** 드롭다운을 붙일 컨테이너. `position: relative`인 팝업 카드를 넘긴다. */
  host: HTMLElement;
  getProjects: () => Project[];
  getSelectedId: () => string | null;
  onPick: (project: Project) => void;
  /** 드롭다운이 열리고 닫힐 때마다 창 높이를 다시 재게 한다. */
  onResize: () => void;
}): ProjectPickerHandle {
  const { button, host, getProjects, getSelectedId, onPick, onResize } = opts;

  const dd = document.createElement("div");
  dd.className = "dropdown";
  dd.hidden = true;
  dd.innerHTML =
    `<div class="dd-search"><span class="sicon">${SEARCH_ICON}</span>` +
    `<input placeholder="프로젝트 검색…" /><span class="cnt"></span></div>` +
    `<div class="dd-list"></div>` +
    `<div class="dd-foot"><span><kbd>↑</kbd><kbd>↓</kbd> 이동</span>` +
    `<span><kbd>↵</kbd> 선택</span><span><kbd>Esc</kbd> 닫기</span></div>`;
  host.appendChild(dd);

  const input = dd.querySelector("input") as HTMLInputElement;
  const list = dd.querySelector(".dd-list") as HTMLElement;
  const cnt = dd.querySelector(".cnt") as HTMLElement;
  let hits: Project[] = [];

  function renderList() {
    const query = input.value;
    const matches = filterProjects(getProjects(), query);
    hits = matches.map((m) => m.project);
    const total = getProjects().length;
    cnt.textContent = query.trim() ? `${hits.length}/${total}` : `${total}개`;

    const selectedId = getSelectedId();
    list.innerHTML = matches.length
      ? matches.map((m) =>
          `<div class="dd-item${m.project.id === selectedId ? " sel" : ""}">` +
          `<span class="dot" style="background:${colorForId(m.project.id)}"></span>` +
          `<span class="nm">${markName(m.project.name, m.range)}</span></div>`).join("")
      : `<div class="dd-empty">일치하는 프로젝트가 없습니다</div>`;

    // 키보드 커서는 현재 선택 위에, 걸러져 사라졌으면 첫 줄에 둔다.
    const items = [...list.querySelectorAll<HTMLElement>(".dd-item")];
    (items.find((el) => el.classList.contains("sel")) ?? items[0])?.classList.add("kbd-focus");
    items.forEach((el, i) => el.addEventListener("click", () => pick(hits[i])));
  }

  function pick(project: Project | undefined) {
    if (!project) return;
    onPick(project);
    render();
    close();
  }

  function move(delta: 1 | -1) {
    const items = [...list.querySelectorAll<HTMLElement>(".dd-item")];
    if (items.length === 0) return;
    const current = items.findIndex((el) => el.classList.contains("kbd-focus"));
    const next = (current + delta + items.length) % items.length;
    items.forEach((el) => el.classList.remove("kbd-focus"));
    items[next].classList.add("kbd-focus");
    items[next].scrollIntoView({ block: "nearest" });
  }

  function focusedIndex(): number {
    return [...list.querySelectorAll<HTMLElement>(".dd-item")]
      .findIndex((el) => el.classList.contains("kbd-focus"));
  }

  function open() {
    dd.hidden = false;
    input.value = ""; // 열 때마다 검색어는 비운다
    renderList();
    input.focus();
    onResize();
  }

  function close() {
    if (dd.hidden) return;
    dd.hidden = true;
    onResize();
  }

  function render() {
    const p = getProjects().find((x) => x.id === getSelectedId());
    const name = button.querySelector("[data-proj-name]") as HTMLElement;
    const dot = button.querySelector(".dot") as HTMLElement;
    name.textContent = p ? p.name : "프로젝트 선택";
    dot.style.background = p ? colorForId(p.id) : "transparent";
  }

  button.addEventListener("click", () => { dd.hidden ? open() : close(); });
  input.addEventListener("input", renderList);
  input.addEventListener("keydown", (e) => {
    if (e.key === "ArrowDown" || e.key === "ArrowUp") {
      e.preventDefault();
      move(e.key === "ArrowDown" ? 1 : -1);
    } else if (e.key === "Enter") {
      // Ctrl+Enter는 제출이므로 여기서 가로채지 않는다(document 핸들러로 넘긴다).
      if (e.ctrlKey) return;
      e.preventDefault();
      pick(hits[focusedIndex()]);
    } else if (e.key === "Escape") {
      e.preventDefault();
      close();
    }
  });

  return {
    render,
    close,
    isOpen: () => !dd.hidden,
    bottom: () => (dd.hidden ? 0 : Math.ceil(dd.getBoundingClientRect().bottom)),
  };
}
```

- [ ] **Step 3: `index.html`의 프로젝트 버튼에 이름 슬롯을 준다**

`src/quickadd/index.html` 33-37행의 `<button id="projBtn">` 안쪽을 바꾸고, 40행의 `<div id="dropdown" class="dropdown" hidden></div>`는 **지운다**(picker가 직접 만든다).

```html
<button id="projBtn" class="proj-select" type="button">
  <span id="projDot" class="dot"></span>
  <span data-proj-name id="projName">프로젝트 선택</span>
  <span class="chev">▾</span>
</button>
```

- [ ] **Step 4: `main.ts`를 picker로 갈아끼운다**

`src/quickadd/main.ts`에서 다음을 **지운다**: `projName`·`projDot`·`dropdown` 상수(25-27행 — picker가 직접 찾는다), `setDropdownOpen`(80-84행), `renderSelected`(179-183행), `renderDropdown`(185-212행), `projBtn.onclick`과 `projBtn` keydown(615-624행). 마지막 것을 지우면 Global Constraints에 적어둔 기존 타입 에러도 함께 사라진다.

대신 파일 위쪽에 import와 picker 생성을 넣는다:

```ts
import { createProjectPicker } from "./projectPicker";

const projectPicker = createProjectPicker({
  button: projBtn,
  host: popupEl,
  getProjects: () => projects,
  getSelectedId: () => selectedId,
  onPick: (p) => {
    selectedId = p.id;
    members = [];
    membersLoadedForProject = null;
    assigneeIds = [];
    renderChips();
    titleEl.focus();
    // 즉시 저장 — 안 그러면 포커스로 다시 도는 load()가 last_project_id를
    // 이 창에서 바꾸기 전 값으로 되돌린다.
    setLastProject(p.id).catch((err) => console.error("setLastProject failed:", err));
  },
  onResize: () => resizeToFit(),
});
```

`popupEl`은 58행에 이미 있다. **`projectPicker` 선언은 `popupEl`·`resizeToFit` 정의 뒤에 두어야 한다**(`resizeToFit`은 함수 선언이라 호이스팅되지만 `popupEl`은 `const`다).

그리고 `dropdown`을 참조하던 나머지를 고친다:

| 위치 | 지금 | 바꿀 것 |
|---|---|---|
| `resizeToFit` (69-71행) | `if (!dropdown.hidden) { height = Math.max(height, ...dropdown...bottom); }` | `height = Math.max(height, projectPicker.bottom());` |
| `resetFields` (591행) | `setDropdownOpen(false);` | `projectPicker.close();` |
| `load()` (611-612행) | `renderSelected(); renderDropdown();` | `projectPicker.render();` |
| Ctrl+Enter 핸들러 (656행) | `setDropdownOpen(false);` | `projectPicker.close();` |
| `titleEl` Escape (638행) | `if (!dropdown.hidden) { setDropdownOpen(false); return; }` | `if (projectPicker.isOpen()) { projectPicker.close(); return; }` |
| 날짜 단축키 가드 (661행) | `shortcut && !openPopover && dropdown.hidden` | `shortcut && !openPopover && !projectPicker.isOpen()` |
| `qaClose` 핸들러 (670행) | `setDropdownOpen(false);` | `projectPicker.close();` |
| `select-project` 리스너 (716-717행) | `renderSelected(); renderDropdown();` | `projectPicker.render();` |

- [ ] **Step 5: 타입 검사와 전체 테스트를 돌린다**

Run: `pnpm build && pnpm test`
Expected: 빌드 성공, 기존 테스트 전부 PASS

- [ ] **Step 6: 앱을 띄워 눈으로 확인한다**

Run: `pnpm tauri dev` → F1로 빠른 추가를 연다. 다음을 확인한다:

1. 하단 프로젝트 버튼을 누르면 드롭다운이 **아래로** 열리고 커서가 검색창에 있다.
2. 오른쪽 위에 `N개`가 보이고, 현재 선택된 프로젝트에 ✓가 있다.
3. 프로젝트 이름 일부를 치면 목록이 좁혀지고 맞은 글자가 파랗게 강조된다. 개수가 `2/10` 꼴로 바뀐다.
4. 초성(예: `ㅍ`)을 쳐도 걸린다.
5. 없는 이름을 치면 `일치하는 프로젝트가 없습니다`.
6. `↑` `↓`로 커서가 움직이고 `↵`로 선택되며 버튼 라벨과 점 색이 바뀐다.
7. `Esc`로 닫힌다. 드롭다운을 열고 닫을 때 **창 높이가 따라 늘고 준다**.
8. 드롭다운이 열린 채로 `Ctrl+Enter`를 누르면 작업이 등록된다(Enter만으로는 선택, Ctrl+Enter는 제출).

- [ ] **Step 7: CHANGELOG 한 줄과 함께 커밋한다**

`CHANGELOG.md`의 `## [Unreleased]` 아래 `### 추가`에 넣는다(`### 추가`가 없으면 만든다):

```
- 빠른 추가의 프로젝트 목록에서 이름으로 검색할 수 있습니다 (초성 검색도 됩니다)
```

```bash
git add src/quickadd/projectPicker.ts src/quickadd/main.ts src/quickadd/index.html src/shared/app.css CHANGELOG.md
git commit -m "feat: 빠른 추가 프로젝트 목록에 검색 추가"
```

---

### Task 3: 폼 상태를 순수 모듈로 분리

동작은 하나도 바뀌지 않는다. `main.ts`에서 DOM을 모르는 부분만 들어낸다.

**Files:**
- Create: `src/quickadd/state.ts`
- Test: `src/quickadd/state.test.ts`
- Modify: `src/quickadd/main.ts`

**Interfaces:**
- Consumes: `DatePresetKey`, `DATE_PRESETS`, `resolveDatePreset`, `shiftIsoDate` (`src/shared/datePresets.ts`), `Priority`, `StateGroup` (`src/shared/planeIcons.ts`), `Member` (`src/shared/types.ts`)
- Produces:
  ```ts
  type DateChoice = DatePresetKey | "custom";
  interface FormState {
    selectedId: string | null;
    members: Member[];
    membersLoadedForProject: string | null;
    assigneeIds: string[];
    startChoice: DateChoice; startCustomDate: string;
    dueChoice: DateChoice;   dueCustomDate: string;
    priority: Priority; stateGroup: StateGroup;
  }
  function createFormState(): FormState
  function dateChoiceLabel(choice: DateChoice, custom: string): string
  function resolveDateChoice(choice: DateChoice, custom: string): string
  function shiftDateField(s: FormState, kind: "start" | "due", delta: number): void
  function toggleAssignee(s: FormState, id: string): void
  function setSingleAssignee(s: FormState, member: Member): void
  function resetFormFields(s: FormState): void
  ```

- [ ] **Step 1: 실패하는 테스트를 쓴다**

Create `src/quickadd/state.test.ts`:

```ts
import { describe, it, expect } from "vitest";
import {
  createFormState, resolveDateChoice, shiftDateField,
  toggleAssignee, setSingleAssignee, resetFormFields,
} from "./state";
import type { Member } from "../shared/types";

const M = (id: string, is_me = false): Member => ({ id, display_name: id, is_me });

describe("shiftDateField — 시작일과 마감일이 교차하지 않는다", () => {
  it("시작일을 마감일 뒤로 밀면 마감일이 따라간다", () => {
    const s = createFormState();
    s.startChoice = "custom"; s.startCustomDate = "2026-01-10";
    s.dueChoice = "custom"; s.dueCustomDate = "2026-01-10";
    shiftDateField(s, "start", 1);
    expect(resolveDateChoice(s.startChoice, s.startCustomDate)).toBe("2026-01-11");
    expect(resolveDateChoice(s.dueChoice, s.dueCustomDate)).toBe("2026-01-11");
  });
  it("마감일을 시작일 앞으로 당기면 시작일이 따라온다", () => {
    const s = createFormState();
    s.startChoice = "custom"; s.startCustomDate = "2026-01-10";
    s.dueChoice = "custom"; s.dueCustomDate = "2026-01-10";
    shiftDateField(s, "due", -1);
    expect(resolveDateChoice(s.dueChoice, s.dueCustomDate)).toBe("2026-01-09");
    expect(resolveDateChoice(s.startChoice, s.startCustomDate)).toBe("2026-01-09");
  });
  it("교차하지 않으면 상대 날짜를 건드리지 않는다", () => {
    const s = createFormState();
    s.startChoice = "custom"; s.startCustomDate = "2026-01-10";
    s.dueChoice = "custom"; s.dueCustomDate = "2026-01-20";
    shiftDateField(s, "start", 1);
    expect(resolveDateChoice(s.dueChoice, s.dueCustomDate)).toBe("2026-01-20");
  });
  it("날짜를 옮기면 프리셋에서 custom으로 넘어간다", () => {
    const s = createFormState();
    expect(s.startChoice).toBe("today");
    shiftDateField(s, "start", 1);
    expect(s.startChoice).toBe("custom");
  });
});

describe("담당자", () => {
  it("toggleAssignee는 넣고 뺀다", () => {
    const s = createFormState();
    toggleAssignee(s, "a");
    expect(s.assigneeIds).toEqual(["a"]);
    toggleAssignee(s, "b");
    expect(s.assigneeIds).toEqual(["a", "b"]);
    toggleAssignee(s, "a");
    expect(s.assigneeIds).toEqual(["b"]);
  });
  it("setSingleAssignee는 선택을 통째로 바꾼다", () => {
    const s = createFormState();
    s.assigneeIds = ["a", "b"];
    setSingleAssignee(s, M("c"));
    expect(s.assigneeIds).toEqual(["c"]);
  });
  it('"나"를 고르면 빈 배열로 되돌린다 — 서버가 호출자에게 할당한다', () => {
    const s = createFormState();
    s.assigneeIds = ["a"];
    setSingleAssignee(s, M("me", true));
    expect(s.assigneeIds).toEqual([]);
  });
});

describe("resetFormFields", () => {
  it("담당자·날짜·우선순위·상태를 기본값으로 되돌린다", () => {
    const s = createFormState();
    s.assigneeIds = ["a"];
    s.startChoice = "custom"; s.startCustomDate = "2026-01-01";
    s.priority = "urgent";
    s.stateGroup = "started";
    resetFormFields(s);
    expect(s.assigneeIds).toEqual([]);
    expect(s.startChoice).toBe("today");
    expect(s.startCustomDate).toBe("");
    expect(s.priority).toBe("medium");
    expect(s.stateGroup).toBe("unstarted");
  });
  it("고른 프로젝트와 멤버 목록은 남긴다 — 연달아 등록할 때 다시 고르게 하지 않는다", () => {
    const s = createFormState();
    s.selectedId = "proj-1";
    s.members = [M("a")];
    s.membersLoadedForProject = "proj-1";
    resetFormFields(s);
    expect(s.selectedId).toBe("proj-1");
    expect(s.members).toHaveLength(1);
    expect(s.membersLoadedForProject).toBe("proj-1");
  });
});
```

- [ ] **Step 2: 테스트를 돌려 실패를 확인한다**

Run: `pnpm vitest run src/quickadd/state.test.ts`
Expected: FAIL — `Failed to resolve import "./state"`

- [ ] **Step 3: 구현한다 (`main.ts`에서 옮긴다)**

Create `src/quickadd/state.ts`. 본문은 `main.ts`의 41-52행(상태 변수), 86-124행(`dateChoiceLabel`/`resolveDateChoice`/`shiftDateField`), 337-345행(`toggleAssignee`), 589-603행(`resetFields`)에서 **DOM을 만지지 않는 부분만** 옮긴 것이다.

```ts
import { DATE_PRESETS, resolveDatePreset, shiftIsoDate, type DatePresetKey } from "../shared/datePresets";
import type { Priority, StateGroup } from "../shared/planeIcons";
import type { Member } from "../shared/types";

export type DateChoice = DatePresetKey | "custom";

/** 빠른 추가 한 건의 폼 상태. DOM을 모른다 — 두 레이아웃이 같은 것을 그린다. */
export interface FormState {
  selectedId: string | null;
  members: Member[];
  membersLoadedForProject: string | null;
  /** 비어 있으면 "나" — 서버가 호출자에게 할당한다. */
  assigneeIds: string[];
  startChoice: DateChoice;
  /** ISO yyyy-mm-dd. startChoice === "custom"일 때만 쓴다. */
  startCustomDate: string;
  dueChoice: DateChoice;
  dueCustomDate: string;
  priority: Priority;
  stateGroup: StateGroup;
}

export function createFormState(): FormState {
  return {
    selectedId: null,
    members: [],
    membersLoadedForProject: null,
    assigneeIds: [],
    startChoice: "today",
    startCustomDate: "",
    dueChoice: "today",
    dueCustomDate: "",
    priority: "medium",
    stateGroup: "unstarted",
  };
}

export function dateChoiceLabel(choice: DateChoice, custom: string): string {
  if (choice === "custom") return custom || "날짜 선택";
  return DATE_PRESETS.find((d) => d.key === choice)!.label;
}

export function resolveDateChoice(choice: DateChoice, custom: string): string {
  return choice === "custom" ? custom : resolveDatePreset(choice);
}

/** 날짜를 하루씩 옮긴다. ISO yyyy-mm-dd 문자열은 그냥 비교해도 순서가 맞으므로
 *  아래 교차 방지에 Date 파싱이 필요 없다. */
export function shiftDateField(s: FormState, kind: "start" | "due", delta: number): void {
  if (kind === "start") {
    const next = shiftIsoDate(resolveDateChoice(s.startChoice, s.startCustomDate), delta);
    s.startCustomDate = next;
    s.startChoice = "custom";
    // 시작일을 마감일 뒤로 밀면 기간이 뒤집힌다 — 마감일을 끌고 간다.
    if (next > resolveDateChoice(s.dueChoice, s.dueCustomDate)) {
      s.dueCustomDate = next;
      s.dueChoice = "custom";
    }
  } else {
    const next = shiftIsoDate(resolveDateChoice(s.dueChoice, s.dueCustomDate), delta);
    s.dueCustomDate = next;
    s.dueChoice = "custom";
    // 반대 방향으로 같은 보호 — 마감일을 시작일 앞으로 당기면 시작일이 따라온다.
    if (resolveDateChoice(s.startChoice, s.startCustomDate) > next) {
      s.startCustomDate = next;
      s.startChoice = "custom";
    }
  }
}

export function toggleAssignee(s: FormState, id: string): void {
  s.assigneeIds = s.assigneeIds.includes(id)
    ? s.assigneeIds.filter((x) => x !== id)
    : [...s.assigneeIds, id];
}

/** 선택을 이 한 사람으로 바꾼다. "나"를 고르면 빈 배열로 되돌린다 — 명시적 id를
 *  박아두는 것보다, 멤버 목록을 다시 받아도 "고르지 않음"으로 읽히는 게 낫다. */
export function setSingleAssignee(s: FormState, member: Member): void {
  s.assigneeIds = member.is_me ? [] : [member.id];
}

/** 등록 성공 후 폼을 비운다. 고른 프로젝트와 멤버 목록은 남긴다 — 같은 프로젝트에
 *  연달아 등록하는 게 흔하다. */
export function resetFormFields(s: FormState): void {
  s.assigneeIds = [];
  s.startChoice = "today";
  s.startCustomDate = "";
  s.dueChoice = "today";
  s.dueCustomDate = "";
  s.priority = "medium";
  s.stateGroup = "unstarted";
}
```

- [ ] **Step 4: 테스트를 돌려 통과를 확인한다**

Run: `pnpm vitest run src/quickadd/state.test.ts`
Expected: PASS — 9 tests

- [ ] **Step 5: `main.ts`가 이 모듈을 쓰게 고친다**

`src/quickadd/main.ts`에서:
- 41-52행의 상태 변수 선언을 `const state = createFormState();` 하나로 바꾼다. `projects: Project[]`는 폼 상태가 아니므로 `main.ts`에 남긴다.
- 86-124행, 337-345행의 함수 정의를 지우고 `./state`에서 import한다.
- 남은 참조를 `state.` 접두어로 고친다(`assigneeIds` → `state.assigneeIds`, `priority` → `state.priority`, …). 호출부도 인자가 하나 늘어난다: `shiftDateField("start", 1)` → `shiftDateField(state, "start", 1)`, `toggleAssignee(id)` → `toggleAssignee(state, id)`.
- 589-603행 `resetFields()`는 상태 초기화를 `resetFormFields(state)`에 넘기고, DOM 부분(`projectPicker.close()`, `descriptionEl.value = ""`, `setDescVisible(false)`, `clearError()`, `closePopover()`, `renderChips()`)만 남긴다.
- 352-361행 `handleAssigneeItemClick`의 `assigneeIds = m.is_me ? [] : [m.id]`를 `setSingleAssignee(state, m)`로, 519행의 같은 식도 마찬가지로 바꾼다.

- [ ] **Step 6: 타입 검사·테스트·수동 확인**

Run: `pnpm exec tsc --noEmit && pnpm build && pnpm test`
Expected: 타입 에러 없음, 빌드 성공, 전체 PASS

`pnpm tauri dev`로 띄워 **동작이 하나도 안 바뀌었는지** 확인한다: 칩 다섯 개가 전부 열리고 값이 바뀌는지, `PgUp`/`PgDn`으로 시작일이 ±1일 되는지, 시작일을 마감일 뒤로 밀면 마감일이 따라오는지, 등록 후 폼이 비워지되 프로젝트는 남는지.

- [ ] **Step 7: 커밋한다**

내부 리팩터라 CHANGELOG는 건드리지 않는다.

```bash
git add src/quickadd/state.ts src/quickadd/state.test.ts src/quickadd/main.ts
git commit -m "refactor: 빠른 추가 폼 상태를 DOM 없는 모듈로 분리"
```

---

### Task 4: v1 렌더러 분리와 `LayoutHandle` 도입

역시 동작은 안 바뀐다. 칩 행을 그리는 코드를 파일 하나로 옮기고, `main.ts`가 렌더러를 **인터페이스 너머로** 다루게 한다.

**Files:**
- Create: `src/quickadd/layout.ts`
- Create: `src/quickadd/layoutCompact.ts`
- Modify: `src/quickadd/index.html`
- Modify: `src/quickadd/main.ts`

**Interfaces:**
- Consumes: `FormState`, `dateChoiceLabel`, `shiftDateField`, `toggleAssignee`, `setSingleAssignee` (Task 3)
- Produces:
  ```ts
  // layout.ts
  interface LayoutHosts {
    titleTrailing: HTMLElement;              // 제목 줄 오른쪽 끝
    fields: HTMLElement;                     // 필드 영역
    description: HTMLTextAreaElement;        // 설명 textarea(레이아웃이 토글한다)
  }
  interface LayoutContext {
    state: FormState;
    onResize: () => void;                    // 창 높이를 다시 재게 한다
    loadMembers: () => Promise<void>;        // 담당자 목록 지연 로딩
    focusTitle: () => void;
  }
  interface LayoutHandle {
    render(): void;
    closeOverlays(): void;
    resetView(): void;                       // 등록 후 화면 상태만 되돌린다(설명 접기 등)
    readonly width: number;
    overlayBottom(): number;                 // 떠 있는 요소의 아래 끝. 없으면 0.
    destroy(): void;
  }
  // layoutCompact.ts
  function mountCompact(hosts: LayoutHosts, ctx: LayoutContext): LayoutHandle
  ```

- [ ] **Step 1: `index.html`을 host 두 개로 정리한다**

`src/quickadd/index.html`의 17-31행을 다음으로 바꾼다. `.chip-row`와 `#fieldPopover`, 칩 여섯 개는 **렌더러가 만든다** — 여기서 지운다.

```html
<div class="popup-top" data-tauri-drag-region>
  <div class="accent-bar" data-tauri-drag-region></div>
  <input id="title" class="title-input" placeholder="진행 중인 작업을 입력하고 Ctrl+Enter…" autofocus />
  <span id="titleTrailing"></span>
</div>
<textarea id="description" class="description-input" placeholder="설명을 입력하세요…" rows="1" hidden></textarea>
<div id="fieldsHost"></div>
<p class="em-error" id="qaError" hidden></p>
```

- [ ] **Step 2: `layout.ts`에 인터페이스만 쓴다**

Create `src/quickadd/layout.ts`:

```ts
import type { FormState } from "./state";

/** 레이아웃이 채울 자리들. 팝업 카드의 나머지(헤더·제목 입력·에러 줄·푸터)는
 *  레이아웃과 무관하게 index.html에 그대로 있다. */
export interface LayoutHosts {
  /** 제목 줄 오른쪽 끝. 컴팩트는 비워두고, 한눈에 보기는 설명 토글을 넣는다. */
  titleTrailing: HTMLElement;
  /** 필드 영역 — 칩 행 또는 필드 격자가 들어간다. */
  fields: HTMLElement;
  /** 설명 입력. 값은 두 레이아웃이 공유하고 보이기/숨기기만 레이아웃이 정한다. */
  description: HTMLTextAreaElement;
}

/** 레이아웃이 바깥(main.ts)에 요청할 수 있는 것들. */
export interface LayoutContext {
  state: FormState;
  /** 그린 내용의 크기가 바뀌었으니 창 크기를 다시 재라. */
  onResize: () => void;
  /** 담당자 목록을 (필요하면) 받아 `state.members`를 채운다. */
  loadMembers: () => Promise<void>;
  /** 제목 입력으로 포커스를 되돌린다. */
  focusTitle: () => void;
}

export interface LayoutHandle {
  /** 상태가 바뀌었으니 다시 그린다. */
  render(): void;
  /** 열려 있는 팝오버를 모두 닫는다(제출·창 닫기 직전). */
  closeOverlays(): void;
  /** 등록 성공 후 이 레이아웃의 화면 상태를 되돌린다(설명 접기, 팝오버 닫기).
   *  폼 값은 state.ts의 resetFormFields가 따로 되돌린다 — 여기는 화면만이다. */
  resetView(): void;
  /** 이 레이아웃이 요구하는 창 폭(px). */
  readonly width: number;
  /** 카드 밖으로 떠 있는 요소의 아래 끝(px). 없으면 0. */
  overlayBottom(): number;
  /** 이벤트를 떼고 DOM을 비운다(레이아웃 전환 시). */
  destroy(): void;
}
```

- [ ] **Step 3: `layoutCompact.ts`로 칩 행을 옮긴다**

Create `src/quickadd/layoutCompact.ts`. 옮길 대상은 `main.ts`의 다음 부분이다 — **로직은 그대로 두고 위치만 옮긴다.**

- 214-242행 `renderAssigneeChip` / `renderChips`
- 244-264행 `autoResizeDescription` / `setDescVisible`
- 266-271행 `closePopover`
- 273-335행 키보드 커서 헬퍼(`initKeyboardFocus`, `moveKeyboardFocus`, `selectKeyboardFocus`, `keyboardFocusIndex`, `setKeyboardFocusIndex`, `handleDropdownKeydown`)
- 346-492행 팝오버 열기 함수들과 칩 `onclick` 배선
- 494-587행 칩 키보드 계약, `attachWheelCycle` 배선, `handleChipArrowNav`

`mountCompact`는 `hosts.fields.innerHTML`에 아래 마크업을 넣고 위 함수들을 그 안의 노드에 건다.

```ts
const CHIP_ROW_HTML = `
  <div class="chip-row" id="chipRow">
    <button type="button" class="chip" id="chipAssignee"></button>
    <button type="button" class="chip" id="chipStart"></button>
    <button type="button" class="chip" id="chipDue"></button>
    <button type="button" class="chip" id="chipState"></button>
    <button type="button" class="chip" id="chipPriority"></button>
    <button type="button" class="chip chip-desc-toggle" id="chipDesc"></button>
    <div id="fieldPopover" class="field-popover" hidden></div>
  </div>`;

export function mountCompact(hosts: LayoutHosts, ctx: LayoutContext): LayoutHandle {
  hosts.fields.innerHTML = CHIP_ROW_HTML;
  hosts.titleTrailing.innerHTML = ""; // 컴팩트는 제목 줄에 아무것도 두지 않는다

  // 위에 나열한 main.ts 구간을 이 안으로 그대로 옮긴다: 칩 엘리먼트 조회
  // (chipAssignee…chipDesc, fieldPopover), renderAssigneeChip, renderChips,
  // autoResizeDescription, setDescVisible, closePopover, 키보드 커서 헬퍼 6개,
  // openAssigneePopover / openDatePopover / openPriorityPopover / openStatePopover,
  // 칩 onclick 배선, 칩 keydown 배선, attachWheelCycle 5개, handleChipArrowNav,
  // bindTip(chipStart) / bindTip(chipDue).
  // 로직은 손대지 않는다 — 아래 "주의할 것"에 적힌 치환만 한다.

  return {
    render: renderChips,
    closeOverlays: () => { closePopover(); },
    resetView: () => { closePopover(); setDescVisible(false); },
    width: 540,
    overlayBottom: () => (openPopover && !fieldPopover.hidden
      ? Math.ceil(fieldPopover.getBoundingClientRect().bottom)
      : 0),
    destroy: () => {
      hosts.fields.innerHTML = "";
      hosts.titleTrailing.innerHTML = "";
    },
  };
}
```

**주의할 것**

- 모듈 최상단에 있던 상태 변수 `openPopover`, `descVisible`는 `mountCompact` 안의 지역 변수가 된다. 폼 값이 아니라 **이 레이아웃의 화면 상태**이므로 `state.ts`로 보내지 않는다.
- `bindTip`(677-697행)의 칩 툴팁 두 줄(`chipStart`, `chipDue`)도 여기로 온다. `qaClose` 툴팁은 레이아웃과 무관하므로 `main.ts`에 남긴다. `qaTip` 엘리먼트는 `document.getElementById("qaTip")`로 계속 찾는다.
- 담당자 팝오버를 열 때 `listMembers`를 직접 부르던 부분(381-396행)은 `await ctx.loadMembers()`로 바꾼다. 네트워크 호출은 `main.ts`가 갖는다.
- `renderChips` 안의 `titleEl.focus()`는 `ctx.focusTitle()`로, `resizeToFit()`은 `ctx.onResize()`로 바꾼다.
- `destroy`가 `innerHTML = ""`로 노드를 통째로 버리므로 리스너를 하나씩 뗄 필요는 없다.

- [ ] **Step 4: `main.ts`가 렌더러를 인터페이스로 다루게 한다**

`src/quickadd/main.ts`가 다음만 남도록 정리한다:

```ts
const hosts: LayoutHosts = {
  titleTrailing: document.getElementById("titleTrailing")!,
  fields: document.getElementById("fieldsHost")!,
  description: document.getElementById("description") as HTMLTextAreaElement,
};

const ctx: LayoutContext = {
  state,
  onResize: () => resizeToFit(),
  loadMembers: async () => {
    if (!state.selectedId || state.membersLoadedForProject === state.selectedId) return;
    try {
      state.members = await listMembers(state.selectedId);
      state.membersLoadedForProject = state.selectedId;
    } catch (err) {
      state.members = [];
      console.error("listMembers failed:", err);
    }
  },
  focusTitle: () => titleEl.focus(),
};

let layout: LayoutHandle = mountCompact(hosts, ctx);
```

`resizeToFit`은 이제 폭도 레이아웃에서 받는다:

```ts
function resizeToFit() {
  let height = Math.ceil(popupEl.getBoundingClientRect().height);
  height = Math.max(height, layout.overlayBottom(), projectPicker.bottom());
  height += 4; // 테두리 한 픽셀이 잘리지 않게 하는 여유
  win.setSize(new LogicalSize(layout.width, height)).catch((err) => {
    console.error("resizeToFit failed:", err);
  });
}
```

`renderChips()`를 부르던 자리는 전부 `layout.render()`로, `closePopover()`는 `layout.closeOverlays()`로 바꾼다. `resetFields()`는 이제 이렇게 된다 — 설명을 접는 일은 레이아웃이 갖고 있으므로 `layout.resetView()`에 맡긴다:

```ts
function resetFields() {
  resetFormFields(state);
  hosts.description.value = "";
  projectPicker.close();
  layout.resetView();
  layout.render();
  clearError();
}
``` 날짜 단축키 핸들러(652-665행)는 `openPopover`를 직접 보던 가드를 없애고 프로젝트 드롭다운만 본다 — 팝오버 안에서 `PgUp`을 눌러도 날짜가 움직이는 편이 낫고, 레이아웃 내부 상태를 `main.ts`가 알 필요도 없다.

- [ ] **Step 5: 타입 검사·테스트·수동 확인**

Run: `pnpm exec tsc --noEmit && pnpm build && pnpm test`
Expected: 타입 에러 없음, 빌드 성공, 전체 PASS

`pnpm tauri dev`로 띄워 **Task 3 Step 6과 같은 항목**을 다시 확인한다. 추가로: 설명 토글이 열리고 닫히며 창 높이가 따라 변하는지, 칩 위에서 휠을 굴리면 값이 바뀌는지, `←`/`→`로 칩 사이를 오가는지, 시작일/마감일 칩에 호버하면 단축키 툴팁이 뜨는지.

- [ ] **Step 6: 커밋한다**

```bash
git add src/quickadd/layout.ts src/quickadd/layoutCompact.ts src/quickadd/main.ts src/quickadd/index.html
git commit -m "refactor: 빠른 추가 칩 행을 렌더러 모듈로 분리"
```

---

### Task 5: 담당자 인라인 배치 로직

v2의 담당자 행이 "누구를 인라인에 두고 누구를 `+N`으로 접을지" 정하는 순수 함수다. Task 7이 쓴다.

**Files:**
- Create: `src/quickadd/assigneeSlots.ts`
- Test: `src/quickadd/assigneeSlots.test.ts`

**Interfaces:**
- Consumes: `Member` (`src/shared/types.ts`)
- Produces:
  ```ts
  interface AssigneeSlots { inline: Member[]; overflow: Member[] }
  function splitAssigneeSlots(members: Member[], assigneeIds: string[], slots: number): AssigneeSlots
  ```

**규칙(스펙에서):** "나"가 항상 맨 앞. **이미 지정된 담당자는 무조건 인라인에 보인다** — 팝오버에서 고른 사람이 `+N` 뒤로 숨으면 몇 명 붙었는지 세러 다시 열어야 하므로 v2의 목적이 무너진다. 남는 칸은 멤버 목록 순서대로 채운다.

- [ ] **Step 1: 실패하는 테스트를 쓴다**

Create `src/quickadd/assigneeSlots.test.ts`:

```ts
import { describe, it, expect } from "vitest";
import { splitAssigneeSlots } from "./assigneeSlots";
import type { Member } from "../shared/types";

const M = (id: string, is_me = false): Member => ({ id, display_name: id, is_me });
const names = (ms: Member[]) => ms.map((m) => m.id);

// 서버가 주는 순서를 흉내낸다 — "나"가 맨 앞에 있으리라는 보장은 없다.
const MEMBERS = [M("a"), M("b"), M("me", true), M("c"), M("d"), M("e"), M("f")];

describe("splitAssigneeSlots", () => {
  it('"나"를 맨 앞에 두고 목록 순서대로 칸을 채운다', () => {
    const { inline, overflow } = splitAssigneeSlots(MEMBERS, [], 4);
    expect(names(inline)).toEqual(["me", "a", "b", "c"]);
    expect(names(overflow)).toEqual(["d", "e", "f"]);
  });
  it("멤버가 칸보다 적으면 전부 인라인이고 접힌 게 없다", () => {
    const { inline, overflow } = splitAssigneeSlots([M("me", true), M("a")], [], 4);
    expect(names(inline)).toEqual(["me", "a"]);
    expect(overflow).toEqual([]);
  });
  it("뒤쪽에 있는 사람을 지정하면 인라인으로 끌어올린다", () => {
    const { inline, overflow } = splitAssigneeSlots(MEMBERS, ["f"], 4);
    expect(names(inline)).toEqual(["me", "f", "a", "b"]);
    expect(names(overflow)).toEqual(["c", "d", "e"]);
  });
  it("지정된 사람이 칸보다 많으면 인라인이 그만큼 늘어난다", () => {
    const { inline, overflow } = splitAssigneeSlots(MEMBERS, ["a", "b", "c", "d", "e"], 4);
    expect(names(inline)).toEqual(["me", "a", "b", "c", "d", "e"]);
    expect(names(overflow)).toEqual(["f"]);
  });
  it('"나"를 명시적으로 지정해도 중복되지 않는다', () => {
    const { inline } = splitAssigneeSlots(MEMBERS, ["me"], 4);
    expect(names(inline)).toEqual(["me", "a", "b", "c"]);
  });
  it('멤버 목록에 "나"가 없어도 무너지지 않는다', () => {
    const { inline } = splitAssigneeSlots([M("a"), M("b")], [], 4);
    expect(names(inline)).toEqual(["a", "b"]);
  });
  it("멤버 목록이 비면 양쪽 다 빈 배열", () => {
    expect(splitAssigneeSlots([], [], 4)).toEqual({ inline: [], overflow: [] });
  });
});
```

- [ ] **Step 2: 테스트를 돌려 실패를 확인한다**

Run: `pnpm vitest run src/quickadd/assigneeSlots.test.ts`
Expected: FAIL — `Failed to resolve import "./assigneeSlots"`

- [ ] **Step 3: 구현한다**

Create `src/quickadd/assigneeSlots.ts`:

```ts
import type { Member } from "../shared/types";

export interface AssigneeSlots {
  /** 인라인 칩으로 그릴 멤버들. */
  inline: Member[];
  /** "+N"으로 접을 나머지. 비면 "+N" 버튼을 그리지 않는다. */
  overflow: Member[];
}

/** 담당자 행에 누구를 펼치고 누구를 접을지 정한다.
 *  - "나"가 항상 맨 앞이다(기본 담당자이므로).
 *  - **지정된 사람은 무조건 인라인**이다. 접힌 뒤로 숨으면 몇 명 붙었는지 세러
 *    팝오버를 다시 열어야 하고, 그러면 "한눈에 보기"가 아니게 된다.
 *  - 남는 칸은 멤버 목록 순서대로 채운다.
 *  지정된 사람이 `slots`보다 많으면 인라인이 `slots`를 넘는다 — 칸 수는 목표지
 *  상한이 아니다. */
export function splitAssigneeSlots(
  members: Member[],
  assigneeIds: string[],
  slots: number,
): AssigneeSlots {
  const inline: Member[] = [];
  const add = (m: Member | undefined) => {
    if (m && !inline.some((x) => x.id === m.id)) inline.push(m);
  };

  add(members.find((m) => m.is_me));
  for (const id of assigneeIds) add(members.find((m) => m.id === id));
  for (const m of members) {
    if (inline.length >= slots) break;
    add(m);
  }

  const taken = new Set(inline.map((m) => m.id));
  return { inline, overflow: members.filter((m) => !taken.has(m.id)) };
}
```

- [ ] **Step 4: 테스트를 돌려 통과를 확인한다**

Run: `pnpm vitest run src/quickadd/assigneeSlots.test.ts`
Expected: PASS — 7 tests

- [ ] **Step 5: 커밋한다**

```bash
git add src/quickadd/assigneeSlots.ts src/quickadd/assigneeSlots.test.ts
git commit -m "feat: 담당자 인라인/접힘 배치 로직 추가"
```

---

### Task 6: 레이아웃 설정 필드

이 Task가 끝나면 설정 화면에서 값을 고르고 저장할 수 있다. **빠른 추가는 아직 이 값을 보지 않는다** — Task 7이 배선한다. 리뷰할 때 그 점을 감안해라.

**Files:**
- Modify: `src-tauri/src/config.rs` (`Settings` 구조체 11-63행, 기본값 함수 65행 이하)
- Modify: `src-tauri/src/commands.rs` (`get_settings` 213-237행, `save_settings` 240-313행)
- Modify: `src/shared/types.ts` (`SettingsDto`)
- Modify: `src/shared/ipc.ts` (`saveSettings` 5-45행)
- Modify: `src/settings/index.html` (`화면` 섹션, 38-45행)
- Modify: `src/settings/main.ts`

**Interfaces:**
- Produces: `SettingsDto.quickadd_layout: string` — `"compact"` 또는 `"expanded"`. Task 7이 읽는다. `quickadd` 창에 `settings-changed` 이벤트(payload 없음)가 날아온다.

- [ ] **Step 1: Rust 설정 필드를 더한다**

`src-tauri/src/config.rs`의 `Settings` 구조체 맨 끝(`show_delegated_tab` 뒤)에 넣는다:

```rust
    /// 빠른 추가 레이아웃. "compact"(칩 행) | "expanded"(한눈에 보기).
    /// 기본이 compact인 이유: 자동 업데이트로 창 모양이 말없이 바뀌면 안 된다.
    #[serde(default = "default_quickadd_layout")]
    pub quickadd_layout: String,
```

기본값 함수들 옆에 넣는다:

```rust
fn default_quickadd_layout() -> String { "compact".into() }
```

- [ ] **Step 2: 명령에 배선한다**

`src-tauri/src/commands.rs`:

`get_settings`의 `SettingsDto` 리터럴 맨 끝에 한 줄 더한다.

```rust
        quickadd_layout: s.quickadd_layout,
```

`save_settings`의 파라미터 목록 맨 끝(`show_delegated_tab: Option<bool>,` 뒤)에 더한다.

```rust
    quickadd_layout: Option<String>,
```

`if let Some(v) = show_delegated_tab { s.show_delegated_tab = v; }` 바로 아래에 검증을 더한다. 아는 값만 받는다 — `theme`을 다루는 방식과 같다.

```rust
    if let Some(v) = quickadd_layout {
        if v == "compact" || v == "expanded" { s.quickadd_layout = v; }
    }
```

`config::save_settings(&app, &s)?;` **바로 다음 줄**에 이벤트를 쏜다. 빠른 추가 창은 부팅 때 만들어져 숨었다 보였다 할 뿐 재로드되지 않으므로, 알려주지 않으면 설정이 반영되지 않는다.

```rust
    // 빠른 추가 창은 재로드되지 않는다 — 설정이 바뀌었음을 알려 렌더러를 갈아끼우게 한다.
    let _ = app.emit_to("quickadd", "settings-changed", ());
```

`Emitter`는 `commands.rs` 2행에서 이미 import돼 있다.

- [ ] **Step 3: Rust가 컴파일되는지 확인한다**

Run: `cd src-tauri && cargo check`
Expected: 성공 (경고는 무방)

- [ ] **Step 4: 프론트 타입과 IPC에 더한다**

`src/shared/types.ts`의 `SettingsDto` 맨 끝(`show_delegated_tab: boolean;` 뒤):

```ts
  quickadd_layout: string;
```

`src/shared/ipc.ts`의 `saveSettings` 파라미터 맨 끝(`showDelegatedTab?: boolean,` 뒤)과 `invoke` 객체 맨 끝(`showDelegatedTab,` 뒤)에 각각 더한다:

```ts
  quickaddLayout?: string,
```
```ts
    quickaddLayout,
```

- [ ] **Step 5: 설정 화면에 select를 더한다**

`src/settings/index.html`의 `<h2>화면</h2>` 아래, 테마 `<label>` **다음**에 넣는다:

```html
        <label>빠른 추가 화면
          <select id="quickaddLayout">
            <option value="compact">컴팩트 — 칩을 눌러 값 바꾸기</option>
            <option value="expanded">한눈에 보기 — 모든 항목 펼쳐 보기</option>
          </select>
        </label>
```

`src/settings/main.ts`에서 세 곳을 고친다.

엘리먼트 상수(104행 `theme` 옆):
```ts
const quickaddLayout = document.getElementById("quickaddLayout") as HTMLSelectElement;
```

불러오기(222행 `theme.value = s.theme;` 옆):
```ts
quickaddLayout.value = s.quickadd_layout;
```

저장(`saveSettings(...)` 호출의 마지막 인자 `showDelegatedTab.checked,` 뒤):
```ts
      quickaddLayout.value,
```

- [ ] **Step 6: 빌드하고 저장이 남는지 확인한다**

Run: `pnpm exec tsc --noEmit && pnpm build && pnpm test`
Expected: 타입 에러 없음, 빌드 성공, 전체 PASS

`pnpm tauri dev`로 띄워: 설정 › 일반 › 화면에 "빠른 추가 화면" select가 보인다 → `한눈에 보기`로 바꾸고 저장 → 설정 창을 닫았다 다시 연다 → **값이 `한눈에 보기`로 남아 있다.** (빠른 추가 화면은 아직 안 바뀐다 — 정상이다.)

- [ ] **Step 7: 커밋한다**

설정 항목만 생기고 아직 아무 효과가 없으므로 CHANGELOG는 Task 7에서 한 줄로 쓴다.

```bash
git add src-tauri/src/config.rs src-tauri/src/commands.rs src/shared/types.ts src/shared/ipc.ts src/settings/index.html src/settings/main.ts
git commit -m "feat: 빠른 추가 레이아웃 설정 항목 추가"
```

---

### Task 7: v2 렌더러와 레이아웃 전환

**Files:**
- Create: `src/quickadd/layoutExpanded.ts`
- Modify: `src/shared/app.css`
- Modify: `src/quickadd/main.ts`
- Modify: `CHANGELOG.md`

**Interfaces:**
- Consumes: `LayoutHosts`/`LayoutContext`/`LayoutHandle` (Task 4), `splitAssigneeSlots` (Task 5), `resolveDateChoice`/`shiftDateField`/`toggleAssignee`/`setSingleAssignee` (Task 3), `SettingsDto.quickadd_layout` (Task 6), `PRIORITY_ORDER`/`STATE_ORDER`/`priorityIcon`/`priorityLabel`/`stateIcon`/`stateLabel`/`DESCRIPTION_ICON` (`src/shared/planeIcons.ts`), `DATE_PRESETS` (`src/shared/datePresets.ts`), `colorForId` (`src/shared/color.ts`)
- Produces: `function mountExpanded(hosts: LayoutHosts, ctx: LayoutContext): LayoutHandle` — `width`는 **660**.

- [ ] **Step 1: CSS를 옮긴다**

`docs/mockups/quickadd-v2-mockup.html`에서 다음 두 블록을 `src/shared/app.css`의 `.field-popover` 규칙 **다음**으로 옮긴다. 목업과 앱이 같은 CSS 변수를 쓰므로 값을 바꿀 필요가 없다.

- `/* ===== v2: 라벨 + 필드 행 ===== */` 블록 전체 (`.qa2-fields`부터 `.qa2-hint`까지, `.seg-track`·`.seg`·`.qa2-people`·`.person`·`.date-stepper` 포함)
- `.desc-toggle` 규칙 세 줄 (`.desc-toggle`, `:hover`, `.on`)
- `.people-pop` 규칙 (`+N` 팝오버)

목업의 `.popup.v2 { width: 660px; }`는 **옮기지 마라** — 앱에서는 `resizeToFit`이 창 크기를 정하고 `.popup`은 창을 꽉 채운다.

- [ ] **Step 2: `layoutExpanded.ts`를 쓴다**

Create `src/quickadd/layoutExpanded.ts`. 마크업 구조는 목업의 v2 팝업과 같다. 뼈대:

```ts
import { splitAssigneeSlots } from "./assigneeSlots";
import { resolveDateChoice, shiftDateField, toggleAssignee, setSingleAssignee } from "./state";
import type { LayoutHandle, LayoutContext, LayoutHosts } from "./layout";
import { DATE_PRESETS } from "../shared/datePresets";
import { attachWheelCycle } from "../shared/wheelCycle";
import { colorForId } from "../shared/color";
import {
  PRIORITY_ORDER, STATE_ORDER, priorityIcon, priorityLabel, stateIcon, stateLabel,
  DESCRIPTION_ICON,
} from "../shared/planeIcons";

/** 담당자 인라인 칸 수("나" 포함). 660px 폭에 이름 칩 넷과 "+N"이 들어가는 수다. */
const ASSIGNEE_SLOTS = 4;

const FIELDS_HTML = `
  <div class="qa2-fields">
    <div class="qa2-row"><span class="qa2-label">담당자</span>
      <div class="qa2-people" data-people></div>
      <span class="qa2-hint" data-assignee-hint></span></div>
    <div class="qa2-row"><span class="qa2-label">상태</span>
      <div class="seg-track" data-state></div></div>
    <div class="qa2-row"><span class="qa2-label">우선순위</span>
      <div class="seg-track" data-priority></div></div>
    <div class="qa2-row" data-date="start"><span class="qa2-label">시작일</span>
      <div class="seg-track" data-presets></div>
      <div class="date-stepper">
        <button type="button" class="step" data-step="-1">‹</button>
        <span class="val" data-val></span>
        <button type="button" class="step" data-step="1">›</button>
      </div>
      <span class="qa2-hint">PgUp/Dn</span></div>
    <div class="qa2-row" data-date="due"><span class="qa2-label">마감일</span>
      <div class="seg-track" data-presets></div>
      <div class="date-stepper">
        <button type="button" class="step" data-step="-1">‹</button>
        <span class="val" data-val></span>
        <button type="button" class="step" data-step="1">›</button>
      </div>
      <span class="qa2-hint">Ctrl+PgUp/Dn</span></div>
  </div>
  <div class="people-pop" data-people-pop hidden></div>`;

export function mountExpanded(hosts: LayoutHosts, ctx: LayoutContext): LayoutHandle {
  hosts.fields.innerHTML = FIELDS_HTML;
  hosts.titleTrailing.innerHTML =
    `<button type="button" class="desc-toggle" data-desc-toggle>${DESCRIPTION_ICON} 설명</button>`;

  // 아래 "각 행의 규칙"대로 render()를 하나 만들고(다섯 행을 매번 다시 그린다)
  // 각 행 컨테이너에 이벤트를 건다. 마운트 끝에서 render() 한 번,
  // ctx.loadMembers().then(render) 한 번을 부른다.
}
```

**각 행의 규칙**

*상태 / 우선순위* — `STATE_ORDER` / `PRIORITY_ORDER` 순서대로 `<button class="seg">`를 만든다. 아이콘과 라벨은 `stateIcon`/`stateLabel`, `priorityIcon`/`priorityLabel`을 쓴다(새 상수를 만들지 마라). 현재 값에 `.on`. 클릭하면 `ctx.state.stateGroup`(또는 `.priority`)을 그 값으로 두고 `render()`.

*시작일 / 마감일* — `DATE_PRESETS` 세 개를 `<button class="seg" data-preset="{key}">`로 그린다. 해당 행의 `choice`가 그 프리셋 키와 같을 때만 `.on` — **`choice === "custom"`이면 셋 다 꺼진다.** 스테퍼의 `[data-val]`에는 `dateChoiceLabel(choice, custom)`이 아니라 **해석된 실제 날짜**를 넣는다:

```ts
const iso = resolveDateChoice(choice, custom);       // "2026-08-10"
const dow = "일월화수목금토"[new Date(iso + "T00:00").getDay()];
valEl.innerHTML = `${iso}<span class="dow">(${dow})</span>`;
```

`‹` `›` 버튼은 `shiftDateField(ctx.state, kind, ∓1)` 후 `render()`. 날짜 텍스트를 클릭하면 네이티브 선택기를 연다:

```ts
const picker = document.createElement("input");
picker.type = "date";
picker.style.cssText = "position:absolute;opacity:0;pointer-events:none";
row.appendChild(picker);
valEl.addEventListener("click", () => {
  picker.value = resolveDateChoice(choice(), custom());
  picker.showPicker();
});
picker.addEventListener("change", () => {
  if (!picker.value) return;
  if (kind === "start") { ctx.state.startChoice = "custom"; ctx.state.startCustomDate = picker.value; }
  else { ctx.state.dueChoice = "custom"; ctx.state.dueCustomDate = picker.value; }
  render();
});
```

*담당자* — `splitAssigneeSlots(ctx.state.members, ctx.state.assigneeIds, ASSIGNEE_SLOTS)`로 나눈다. 각 인라인 멤버는:

```html
<button type="button" class="person{ 선택됐으면 ' on'}" data-member-id="{id}">
  <span class="avatar" style="background:{colorForId(id)}">{이름 첫 글자}</span> {표시 이름}
</button>
```

선택 판정은 v1과 같다 — `assigneeIds.includes(m.id) || (m.is_me && assigneeIds.length === 0)`. 클릭은 `setSingleAssignee(state, m)`, **Ctrl+클릭은** `toggleAssignee(state, m.id)`. `overflow`가 비어 있지 않으면 `<button class="person more" data-more>+{overflow.length}</button>`를 붙이고, 누르면 `[data-people-pop]`을 담당자 칩 바로 아래에 띄운다(목업의 `place()` 함수와 같은 계산). 팝오버 항목은 `.dd-item` 문법을 쓴다.

우측 `[data-assignee-hint]`는 지정 인원이 2명 이상이면 `N명 지정됨`, 아니면 `Ctrl+클릭 다중`.

`mountExpanded`가 처음 그릴 때 멤버 목록이 비어 있을 수 있으므로, 마운트 직후 `ctx.loadMembers().then(render)`를 한 번 부른다. **v1과 달리 담당자 목록이 항상 보이므로 지연 로딩이 아니라 즉시 로딩이다.**

*설명 토글* — `[data-desc-toggle]` 클릭 시 `hosts.description.hidden`을 뒤집고 버튼에 `.on`을 토글, 보이면 높이를 내용에 맞추고 포커스를 준다. `destroy`에서 `hosts.description.hidden = true`로 되돌린다.

*휠* — `attachWheelCycle`을 **세그먼트가 아니라 트랙**(`[data-state]`, `[data-priority]`)에 건다. 상태·우선순위는 순서 있는 척도이므로 v1처럼 양 끝에서 멈춘다(순환하지 않는다):

```ts
attachWheelCycle(stateTrack, () => STATE_ORDER.length, (delta) => {
  const i = STATE_ORDER.indexOf(ctx.state.stateGroup);
  ctx.state.stateGroup = STATE_ORDER[Math.max(0, Math.min(STATE_ORDER.length - 1, i + delta))];
  render();
});
```

날짜 행의 스테퍼에도 `attachWheelCycle(stepper, () => 2, (d) => { shiftDateField(ctx.state, kind, d); render(); })`를 건다.

**반환값**

```ts
return {
  render,
  closeOverlays: () => { peoplePop.hidden = true; },
  resetView: () => { peoplePop.hidden = true; setDescVisible(false); },
  width: 660,
  overlayBottom: () => (peoplePop.hidden ? 0 : Math.ceil(peoplePop.getBoundingClientRect().bottom)),
  destroy: () => {
    hosts.fields.innerHTML = "";
    hosts.titleTrailing.innerHTML = "";
    hosts.description.hidden = true;
  },
};
```

- [ ] **Step 3: `main.ts`가 설정에 따라 렌더러를 고르게 한다**

`src/quickadd/main.ts`에서 `let layout: LayoutHandle = mountCompact(hosts, ctx);`를 다음으로 바꾼다:

```ts
import { mountExpanded } from "./layoutExpanded";

type LayoutKind = "compact" | "expanded";
let layoutKind: LayoutKind = "compact";
let layout: LayoutHandle = mountCompact(hosts, ctx);

/** 설정이 가리키는 레이아웃으로 갈아끼운다. 폼 상태는 state.ts에 있으므로
 *  작성 중이던 초안은 그대로 살아남는다. */
function applyLayout(kind: LayoutKind) {
  if (kind === layoutKind) return;
  layout.destroy();
  layoutKind = kind;
  layout = kind === "expanded" ? mountExpanded(hosts, ctx) : mountCompact(hosts, ctx);
  layout.render();
  resizeToFit();
}
```

`load()`에서 설정을 읽은 뒤 적용한다(`applyTheme(settings.theme);` 다음 줄):

```ts
  applyLayout(settings.quickadd_layout === "expanded" ? "expanded" : "compact");
```

그리고 설정 저장 이벤트를 듣는다. `win.listen<string>("select-project", …)` 옆에 둔다:

```ts
// 설정 창이 저장하면 즉시 반영한다 — 이 창은 재로드되지 않는다.
win.listen("settings-changed", async () => {
  const s = await getSettings();
  applyTheme(s.theme);
  applyLayout(s.quickadd_layout === "expanded" ? "expanded" : "compact");
});
```

- [ ] **Step 4: 빌드와 테스트**

Run: `pnpm exec tsc --noEmit && pnpm build && pnpm test`
Expected: 타입 에러 없음, 빌드 성공, 전체 PASS

- [ ] **Step 5: 앱을 띄워 목업과 대조한다**

Run: `pnpm tauri dev`. `docs/mockups/quickadd-v2-mockup.html`을 브라우저로 나란히 열어두고 비교한다.

1. 설정에서 `한눈에 보기`로 바꾸고 저장하면 **빠른 추가 창이 그 자리에서 넓어지고**(660px) 다섯 줄이 나타난다. 설정 창을 닫을 필요 없다.
2. 담당자 행에 "나"가 맨 앞에 있고 최대 4칸까지 인라인, 나머지는 `+N`.
3. 담당자를 클릭하면 그 사람만 지정되고, `Ctrl`+클릭하면 여러 명이 켜진다. 오른쪽 힌트가 `2명 지정됨`으로 바뀐다.
4. `+N`을 누르면 팝오버가 담당자 칩 아래에 뜨고, 거기서 고른 사람이 **인라인 칩으로 올라온다.**
5. 상태·우선순위 세그먼트를 누르면 즉시 바뀌고 아이콘 색이 목업과 같다.
6. 날짜 프리셋을 누르면 그 프리셋만 켜지고 스테퍼 값이 따라 바뀐다. `‹` `›`로 하루씩 움직이면 **프리셋 세 개가 모두 꺼진다.**
7. 시작일을 마감일 뒤로 밀면 마감일이 따라온다.
8. 날짜 텍스트를 클릭하면 달력이 열린다.
9. 설명 토글이 제목 줄 오른쪽에 있고, 누르면 입력칸이 나타나며 창이 높아진다.
10. `Ctrl+Enter`로 등록되고 폼이 비워진다. 등록된 작업을 Plane에서 열어 담당자·상태·우선순위·날짜가 **고른 대로 들어갔는지** 확인한다.
11. 설정을 `컴팩트`로 되돌리면 창이 540px로 줄고 칩 행이 돌아온다. **제목에 쳐 둔 글자는 그대로 남아 있다.**
12. 라이트 테마에서도 1-11을 한 번 훑는다.

- [ ] **Step 6: CHANGELOG 한 줄과 함께 커밋한다**

`## [Unreleased]`의 `### 추가`에 더한다:

```
- 설정에서 빠른 추가를 "한눈에 보기"로 바꾸면 담당자·상태·우선순위·시작일·마감일을 접지 않고 한 화면에서 고를 수 있습니다
```

```bash
git add src/quickadd/layoutExpanded.ts src/quickadd/main.ts src/shared/app.css CHANGELOG.md
git commit -m "feat: 빠른 추가 '한눈에 보기' 레이아웃 추가"
```

---

### Task 8: v2 키보드 계약

마우스로 다 되는 상태에서, 키보드만으로도 되게 만든다.

**Files:**
- Modify: `src/quickadd/layoutExpanded.ts`

**Interfaces:**
- Consumes: Task 7의 `mountExpanded` 내부 구조. 새로 내보내는 것은 없다.

**계약(스펙 표)**

| 키 | 하는 일 |
|---|---|
| `Tab` / `Shift+Tab` | 행 사이 이동 |
| `←` `→` | 행 안에서 세그먼트/담당자 칩 이동 |
| `Enter` | 포커스한 세그먼트 선택 / 담당자는 단독 지정 |
| `Space` | 담당자 칩 토글 |
| `Esc` | `+N` 팝오버 닫기 (없으면 창 닫기 — `main.ts`가 처리) |

`Ctrl+Enter`(제출), `PgUp/Dn`, `Ctrl+PgUp/Dn`은 `main.ts`의 document 핸들러가 이미 잡으므로 **여기서 건드리지 마라.**

- [ ] **Step 1: 행 사이 Tab 이동을 만든다**

각 행에서 **포커스를 받는 것은 하나**여야 `Tab`이 행 단위로 움직인다. 행마다 "현재 활성" 요소만 `tabindex="0"`, 나머지는 `tabindex="-1"`로 둔다(roving tabindex). `render()` 끝에서 매번 다시 매긴다:

```ts
/** 행마다 포커스 받는 요소를 하나로 줄인다 — 그래야 Tab이 칩 하나하나가 아니라
 *  행 단위로 움직인다. 활성 요소는 선택된 것, 없으면 첫 번째. */
function applyRovingTabindex(row: HTMLElement) {
  const items = [...row.querySelectorAll<HTMLElement>(".seg, .person")];
  if (items.length === 0) return;
  const active = items.find((el) => el.classList.contains("on")) ?? items[0];
  items.forEach((el) => { el.tabIndex = el === active ? 0 : -1; });
}
```

- [ ] **Step 2: 행 안 화살표 이동을 건다**

행 컨테이너에 keydown 하나를 걸어 위임한다:

```ts
function handleRowKeydown(row: HTMLElement, e: KeyboardEvent) {
  const items = [...row.querySelectorAll<HTMLElement>(".seg, .person")];
  const current = items.indexOf(document.activeElement as HTMLElement);
  if (current === -1) return;
  if (e.key === "ArrowLeft" || e.key === "ArrowRight") {
    e.preventDefault();
    // 양 끝에서 멈춘다 — 순환하면 어디까지 왔는지 감이 안 온다.
    const next = Math.max(0, Math.min(items.length - 1, current + (e.key === "ArrowRight" ? 1 : -1)));
    items[next].tabIndex = 0;
    items[current].tabIndex = -1;
    items[next].focus();
  }
}
```

- [ ] **Step 3: Enter / Space를 건다**

`.seg`는 `<button>`이므로 `Enter`와 `Space` 둘 다 기본 `click`이 난다 — 세그먼트 행은 **추가 작업이 없다.**

담당자 행만 다르다. `.person`도 `<button>`이라 `Enter`/`Space`가 click을 내는데, `Space`는 토글이어야 하므로 `keydown`에서 가로챈다:

```ts
peopleRow.addEventListener("keydown", (e) => {
  const el = document.activeElement as HTMLElement;
  const id = el?.dataset.memberId;
  if (!id) return;
  if (e.key === " ") {
    e.preventDefault(); // 기본 click(=단독 지정)이 나지 않게 막는다
    toggleAssignee(ctx.state, id);
    render();
    // render()가 DOM을 다시 만들므로 같은 자리에 포커스를 되돌린다.
    peopleRow.querySelector<HTMLElement>(`[data-member-id="${id}"]`)?.focus();
  }
});
```

`Enter`는 버튼 기본 동작(click → `setSingleAssignee`)에 맡긴다.

- [ ] **Step 4: `+N` 팝오버의 Esc를 건다**

팝오버가 열려 있을 때 `Esc`는 팝오버만 닫고 창은 닫지 않아야 한다. `main.ts`의 `titleEl` Escape 핸들러가 `layout.closeOverlays()`를 먼저 부르도록, `LayoutHandle`에 이미 있는 `closeOverlays`를 쓴다. `layoutExpanded` 쪽에서는 팝오버 자체에 keydown을 걸어 닫고 `+N` 버튼으로 포커스를 되돌린다.

```ts
peoplePop.addEventListener("keydown", (e) => {
  if (e.key !== "Escape") return;
  e.preventDefault();
  peoplePop.hidden = true;
  ctx.onResize();
  moreBtn?.focus();
});
```

- [ ] **Step 5: 빌드와 키보드 확인**

Run: `pnpm exec tsc --noEmit && pnpm build && pnpm test`
Expected: 타입 에러 없음, 빌드 성공, 전체 PASS

`pnpm tauri dev`로 띄워 **마우스를 쓰지 않고** 다음을 해본다:

1. 제목을 친다 → `Tab`을 누르면 담당자 행으로, 다시 `Tab`이면 상태 행으로, 계속해서 우선순위 → 시작일 → 마감일 → 프로젝트 버튼 → 추가 버튼 순으로 간다. `Shift+Tab`으로 되돌아온다.
2. 상태 행에서 `←` `→`로 세그먼트를 옮기고 `Enter`로 고른다.
3. 담당자 행에서 `→`로 옮기고 `Enter`면 그 사람만, `Space`면 토글로 여러 명이 켜진다.
4. `+N`에서 `Enter`로 팝오버를 열고 `Esc`로 닫으면 포커스가 `+N`으로 돌아온다.
5. 어느 칸에 있든 `PgUp`/`PgDn`으로 시작일이, `Ctrl+PgUp`/`Ctrl+PgDn`으로 마감일이 하루씩 움직인다.
6. 어느 칸에 있든 `Ctrl+Enter`로 등록된다.
7. `Esc`로 창이 닫힌다(팝오버가 열려 있으면 팝오버부터 닫힌다).

- [ ] **Step 6: 커밋한다**

키보드 계약은 새 기능이라기보다 Task 7 기능의 완성이므로 CHANGELOG 줄을 따로 만들지 않는다(`CLAUDE.md`: 같은 릴리스 안에서 아직 태그가 안 나간 항목은 원래 줄에 합친다).

```bash
git add src/quickadd/layoutExpanded.ts
git commit -m "feat: 한눈에 보기 레이아웃 키보드 조작 추가"
```

---

## 자체 검토 결과

**스펙 대조** — 스펙의 각 절이 어느 Task에 들어갔는지:

| 스펙 절 | Task |
|---|---|
| 파일 구조 (`state.ts` / 렌더러 분리) | 3, 4 |
| 렌더러 계약 `LayoutHandle` | 4 |
| v2 레이아웃 — 다섯 행, 행 분리선 | 7 |
| v2 레이아웃 — 담당자 규칙("나" 우선, 지정자 항상 인라인) | 5, 7 |
| v2 레이아웃 — 날짜(프리셋/스테퍼/네이티브 선택기/custom이면 프리셋 꺼짐) | 7 |
| v2 레이아웃 — 설명 토글 | 7 |
| 키보드 (유지분 / v2 변경분 / 휠) | 7(휠), 8 |
| 프로젝트 검색 — 매칭 규칙, 초성, 강조 | 1 |
| 프로젝트 검색 — 드롭다운 UI, 개수, 빈 결과, 키보드 | 2 |
| 창 크기 (`resizeToFit`이 폭도 넘김) | 4, 7 |
| 설정 (Rust 필드/명령/DTO/IPC/화면/이벤트) | 6 |
| CHANGELOG 두 줄 | 2, 7 |

빠진 스펙 요구사항 없음.

**미결정 사항** — 스펙의 "아직 안 정한 것"(담당자 인라인 정렬 순서, `Alt+1..5` 숫자 단축키)은 계획에 넣지 않았다. 스펙이 "정하지 않아도 막히지 않는다"고 명시했고, 계획은 결정된 안(멤버 목록 순서, 숫자 단축키 없음)으로 구현한다.
