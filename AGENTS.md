# Plane Quick Dock — 제품 방향성 (에이전트 필독)

이 문서는 이 프로젝트가 **어디로 가고 있는지**를 정의한다. 기능을 설계·구현·리뷰할 때
항상 이 방향성에 비추어 판단한다. 작업 규칙(CHANGELOG, 릴리스 절차)은 `CLAUDE.md` 참고.

## 비전

Plane(셀프호스팅 프로젝트 관리 도구)을 **창을 띄우지 않고도** 쓰게 만드는 데스크톱 도크.

- **작업자에게는 생산성 도구** — 생각의 흐름을 끊지 않고 작업을 캡처하고,
  오늘 할 일을 한눈에 보고, 클릭 한두 번으로 처리한다.
- **관리자(PM)에게는 관제탑** — 팀 상태·리스크·진행률을 브라우저 없이
  즉시 파악하고, 가벼운 관리 액션(할당, 트리아지)을 바로 수행한다.

현재는 작업자용 2개 창(QuickAdd, 사이드바)만 있지만, 앞으로 **관리자용 창들을
추가**해 두 페르소나를 모두 지원하는 제품으로 성장시킨다.

## 두 페르소나, 두 설계 원칙

### 작업자 (현재 중심)
- **캡처 마찰 최소화**: 전역 단축키 → 입력 → Ctrl+Enter. 3초 안에 등록이 목표.
- **키보드 퍼스트**: 모든 기능은 마우스 없이 조작 가능해야 한다.
- **글랜서블(glanceable)**: 사이드바는 "오늘의 나"에 집중 — 과거·미래·남의 일은
  기본 화면에서 배제한다.
- **낙관적 업데이트**: UI 먼저 반영, 실패 시 롤백. 서버를 기다리게 하지 않는다.

### 관리자 (확장 방향)
- **읽기 우선(read-first)**: 대시보드·요약·리스크 감지가 먼저다. 쓰기 액션은
  검증된 뷰 위에 얹는다.
- **인사이트 > 데이터**: 원본 목록 나열이 아니라 "지금 봐야 할 것"(지연, 정체,
  과부하)을 골라서 보여준다.
- **관리 액션은 가볍게**: 할당·우선순위·마감 조정처럼 한 클릭 액션 위주.
  무거운 편집은 Plane 웹으로 연결한다.

## 로드맵 테마 (우선순위 순)

1. **작업자 흐름 심화** — 캡처·처리 속도를 더 끌어올리는 기능
   (자연어 입력 파싱, 명령 팔레트, 알림/브리핑, 서브태스크·댓글).
2. **데이터 기반 다지기** — 관리자 뷰의 전제 조건. 백그라운드 동기화,
   캐싱, 델타 갱신 (현재는 포커스 시 N+1 전체 재조회).
3. **관리자 읽기 뷰** — 팀 현황 대시보드, 리스크 레이더(지연·정체·긴급 미착수),
   데일리 다이제스트, 사이클/스프린트 진행률.
4. **관리자 쓰기 액션** — 트리아지(미할당 처리), 재할당, 워크로드 밸런싱.

## Plane API 활용 지도

현재는 Projects / Work Items / States / Members만 사용한다. Plane API에는 아직 안 쓰는
리소스가 많고, 로드맵 테마와 이렇게 연결된다:

- **작업자 흐름**: Comments(댓글), Attachments(첨부·스크린샷), Links(URL 연결),
  Labels(라벨 칩·필터), Search(명령 팔레트), 부모-자식 이슈(서브태스크),
  Activity(내 이슈 변경 감지 → 알림), Worklogs(타이머·시간 기록), Stickies(빠른 메모).
- **관리자 뷰**: Cycles(스프린트 진행률), Modules(기능 단위 진행률), Intake(요청
  트리아지), Activity(팀 다이제스트), Estimates+Worklogs(계획 대비 실적), Members(워크로드).
- **주의**: Epics, Initiatives, Teamspaces, Customers 등은 유료 플랜 전용일 수 있다.
  셀프호스팅 CE 인스턴스에서 해당 엔드포인트가 열려 있는지 먼저 확인하고 설계한다.

## 아키텍처 함의

- **멀티 윈도우 확장 전제**: 새 창 추가가 반복될 것이므로 창 생성·단축키·테마·
  IPC 패턴은 재사용 가능하게 유지한다 (`src/<window>/` + `src/shared/`).
- **작업자/관리자 모드**: 설정에서 역할별로 창·단축키를 켜고 끌 수 있어야 한다.
  관리자 기능이 작업자 UX를 무겁게 만들면 안 된다.
- **Plane API가 유일한 백엔드**: 로컬 상태는 캐시일 뿐, 진실은 항상 서버.
  레이트 리밋을 존중한다 (쿨다운·캐싱 패턴 유지).
